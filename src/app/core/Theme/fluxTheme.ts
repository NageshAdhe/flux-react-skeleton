import { createTheme } from '@mui/material/styles';
const theme = createTheme({
  palette: {
    primary: {
      main: '#0070f3', // Customize as needed
    },
    secondary: {
      main: '#ff4081', // Customize as needed
    },
  },
});

export default theme;
